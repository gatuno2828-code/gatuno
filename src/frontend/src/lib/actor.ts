// Actor stub — backend interface is empty until bindgen produces real methods.
// All data queries fall back to mock data via useBackend hooks.
import { useActor } from "@caffeineai/core-infrastructure";

export { useActor };
